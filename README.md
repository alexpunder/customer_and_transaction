## Домашнее задание 1. Создание и нормализация базы данных

По какой-то причине, если открывать ноутбук непосредственно на `github`, то он ломает отображение табличных данных. Чтобы избежать такого эффекта, можно воспользоваться `dev`-инструментом - там всё отлично отображается:  
```
https://github.dev/alexpunder/customer_and_transaction
```
